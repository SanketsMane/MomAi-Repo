#!/bin/bash

echo "🧹 STEP 1: COMPLETE EC2 CLEANUP"
echo "================================"
echo ""
echo "Go to AWS Console → EC2 → Instance (3.111.199.28) → Connect → Session Manager"
echo ""
echo "Copy and paste these cleanup commands:"
echo ""
echo "# Kill all PM2 processes"
echo "sudo pm2 kill 2>/dev/null || true"
echo ""
echo "# Remove all application files"
echo "sudo rm -rf /home/ubuntu/mom-ai"
echo "sudo rm -rf /home/ubuntu/*.tar.gz"
echo "sudo rm -rf /home/ubuntu/.pm2"
echo ""
echo "# Clean caches"
echo "sudo rm -rf /home/ubuntu/.npm"
echo "sudo rm -rf /home/ubuntu/.pnpm-store"
echo "sudo rm -rf /home/ubuntu/.cache"
echo ""
echo "# Clean nginx configs"
echo "sudo rm -f /etc/nginx/sites-available/momdigital*"
echo "sudo rm -f /etc/nginx/sites-enabled/momdigital*"
echo ""
echo "# Stop services"
echo "sudo systemctl stop nginx 2>/dev/null || true"
echo ""
echo "# Kill any stuck processes"
echo "sudo fuser -k 3000/tcp 2>/dev/null || true"
echo "sudo fuser -k 3001/tcp 2>/dev/null || true"
echo ""
echo "# Clean system logs (optional)"
echo "sudo journalctl --vacuum-time=1d"
echo ""
echo "echo '✅ EC2 CLEANUP COMPLETE!'"
echo ""
echo "================================="
echo "✅ After cleanup, run from local:"
echo "✅ ./fresh-deploy.sh"